<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.changheonkim.safekids">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Admin"
            android:label="관리자" />
        <activity
            android:name=".Parents"
            android:label="학부모" />
        <activity
            android:name=".Board"
            android:label="게시판" />
        <activity
            android:name=".Admin_SignUp"
            android:label="회원가입(교사)" />
        <activity
            android:name=".Parents_sign_up"
            android:label="회원가입(학부모)" />
        <activity
            android:name=".FindIdOrPw"
            android:label="ID/PW찾기" />
        <activity
            android:name=".Sign_up_divide"
            android:label="계정 설정" />
        <activity android:name=".ManagementActivity"></activity>
    </application>

</manifest>